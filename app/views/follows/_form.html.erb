<%= form_with model: user.follows_to.build do |f| %>
    <div>
        <%= f.hidden_field :followed_id, value: user.id %>
        <%#= hidden_field_tag :followed_id, user.id %>
    </div>

    <div> 
        <%= f.submit 'Follow', class: 'btn btn-primary' %>
    </div>
<% end %>