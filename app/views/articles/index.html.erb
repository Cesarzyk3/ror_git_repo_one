<div class="container-xl col-6">
  <h1>Articles</h1>

  <% @articles.each do |article| %>
  <div class="container rounded border pb-2 mb-3">
    <h3 class=""><%= article.title %></h3>
    <%= article.body %><br><br>
    <b><%= User.find(article.user_id).email%></b><br>
    <%= link_to 'Show', article, class: 'btn border py-0 px-1' %>
    <%= link_to 'Edit', edit_article_path(article), class: 'btn border py-0 px-1' %>
    <%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn border py-0 px-1' %></td>
  </div>
  <% end %>
  <br>
  <%= link_to 'New Article', new_article_path, class: 'btn border m-0' %>
</div>